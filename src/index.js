import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import VirtualTryOnGlasses from "./VirtualTryOnGlasses";

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
      <VirtualTryOnGlasses />
  </React.StrictMode>
);
